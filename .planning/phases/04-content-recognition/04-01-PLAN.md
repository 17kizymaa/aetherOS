---
phase: 04-content-recognition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - profiles/aetheros/packages.x86_64
  - profiles/aetheros/airootfs/etc/systemd/system/living-archive-scan.service
  - scripts/living-archive.sh
autonomous: true
requirements:
  - RECOG-01
  - RECOG-02
  - RECOG-04

must_haves:
  truths:
    - "ripgrep package added for content search"
    - "Living Archive scan service runs at first boot"
    - "Scan uses file heuristics (extensions, dir names)"
    - "No ML models used in categorization"
  artifacts:
    - path: "profiles/aetheros/packages.x86_64"
      provides: "ripgrep for search functionality"
      contains: "ripgrep"
    - path: "profiles/aetheros/airootfs/etc/systemd/system/living-archive-scan.service"
      provides: "First-boot content scanning service"
      contains: "living-archive"
    - path: "scripts/living-archive.sh"
      provides: "Content categorization using file heuristics"
      contains: "Projects"
---

<objective>
Add ripgrep to packages and create the Living Archive scan service that runs at first boot. Uses file heuristics (not ML) to categorize content into Projects, Photos, Documents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@docs/VM_DEMO_ACCEPTANCE.md
@.planning/phases/04-content-recognition/04-CONTEXT.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Add ripgrep to package list</name>
  <files>profiles/aetheros/packages.x86_64</files>
  <action>
Add `ripgrep` to packages.x86_64 in a new "Phase 4: Content recognition" section.
  </action>
  <verify>
    <automated>cd /home/anphuni/aetherOS && grep -q 'ripgrep' profiles/aetheros/packages.x86_64</automated>
  </verify>
  <done>
ripgrep package added to packages.x86_64
  </done>
</task>

<task type="auto">
  <name>Create Living Archive content scanner script</name>
  <files>scripts/living-archive.sh</files>
  <action>
Create `scripts/living-archive.sh` using shell script boilerplate. Script scans $HOME directories and categorizes by:
- Projects: directories containing .git, - package.json, .cargo/config.toml, etc.
- Photos: extensions .jpg, .jpeg, .png, .gif, .raw, .cr2, .nef
- Documents: extensions .pdf, .doc, .docx, .txt, .md, .org
- Other: fallback category

Uses `rg` (ripgrep) or `find` for file discovery.
  </action>
  <verify>
    <automated>cd /home/anphuni/aetherOS && test -f scripts/living-archive.sh && test -x scripts/living-archive.sh && grep -q 'Projects' scripts/living-archive.sh</automated>
  </verify>
  <done>
living-archive.sh created and executable with heuristic-based categorization
  </done>
</task>

<task type="auto">
  <name>Create Living Archive systemd service</name>
  <files>profiles/aetheros/airootfs/etc/systemd/system/living-archive-scan.service</files>
  <action>
Create systemd service in airootfs that runs the Living Archive scanner at first boot. Uses ConditionFirstBoot=yes like create-demo-user service.
  </action>
  <verify>
    <automated>cd /home/anphuni/aetherOS && test -f profiles/aetheros/airootfs/etc/systemd/system/living-archive-scan.service && grep -q 'ConditionFirstBoot' profiles/aetheros/airootfs/etc/systemd/system/living-archive-scan.service</automated>
  </verify>
  <done>
Living Archive service created with first-boot condition
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scanner -> filesystem | Scans user directories at first boot. Read-only access. |
| Service -> systemd | Service runs with user privileges via ConditionFirstBoot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | living-archive.sh | accept | Script reads file metadata only, outputs to user-owned manifests |
| T-04-02 | Denial of Service | scan service | mitigate | Quick scan, early exit on large directories, non-blocking |
| T-04-03 | Tampering | systemd service | mitigate | Config is committed to repo |
</threat_model>

<verification>
After all tasks:
1. ripgrep in packages.x86_64
2. living-archive.sh exists and is executable
3. living-archive-scan.service exists with ConditionFirstBoot
4. G0-G2 validation passes
</verification>

<success_criteria>
- ripgrep package added
- living-archive.sh script for heuristic categorization
- systemd service runs at first boot
- No ML models used
</success_criteria>

<output>
Create `.planning/phases/04-content-recognition/04-01-SUMMARY.md` when done
</output>