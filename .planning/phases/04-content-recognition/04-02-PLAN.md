---
phase: 04-content-recognition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - profiles/aetheros/airootfs/usr/bin/living-archive-search
  - profiles/aetheros/airootfs/etc/skel/Desktop/living-archive.desktop
autonomous: true
requirements:
  - RECOG-03

must_haves:
  truths:
    - "Living Archive launcher exists on desktop"
    - "Search script uses ripgrep for content search"
    - "Launcher opens categorized view"
  artifacts:
    - path: "profiles/aetheros/airootfs/usr/bin/living-archive-search"
      provides: "Search script for Living Archive"
      contains: "rg"
    - path: "profiles/aetheros/airootfs/etc/skel/Desktop/living-archive.desktop"
      provides: "Desktop launcher for Living Archive"
      contains: "living-archive-search"
---

<objective>
Create the Living Archive search script and desktop launcher for the unified view of local content. Uses ripgrep for fast searching.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-content-recognition/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Create Living Archive search script</name>
  <files>profiles/aetheros/airootfs/usr/bin/living-archive-search</files>
  <action>
Create `profiles/aetheros/airootfs/usr/bin/living-archive-search` - a search script using ripgrep. Should present a simple interface to search categorized content.
  </action>
  <verify>
    <automated>cd /home/anphuni/aetherOS && test -f profiles/aetheros/airootfs/usr/bin/living-archive-search && grep -q 'rg' profiles/aetheros/airootfs/usr/bin/living-archive-search</automated>
  </verify>
  <done>Living Archive search script created with ripgrep</done>
</task>

<task type="auto">
  <name>Create Living Archive desktop launcher</name>
  <files>profiles/aetheros/airootfs/etc/skel/Desktop/living-archive.desktop</files>
  <action>
Create `profiles/aetheros/airootfs/etc/skel/Desktop/living-archive.desktop` - desktop entry that launches the Living Archive search. Uses zenity or terminal-based interface.
  </action>
  <verify>
    <automated>cd /home/anphuni/aetherOS && test -f profiles/aetheros/airootfs/etc/skel/Desktop/living-archive.desktop && grep -q 'living-archive-search' profiles/aetheros/airootfs/etc/skel/Desktop/living-archive.desktop</automated>
  </verify>
  <done>Living Archive desktop launcher created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search -> filesystem | Reads user files, outputs to user session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | living-archive-search | accept | Script runs with user privileges |
| T-04-05 | Tampering | desktop launcher | mitigate | Config is committed to repo |
</threat_model>

<verification>
After all tasks:
1. living-archive-search exists and uses ripgrep
2. living-archive.desktop launcher exists
3. G0-G2 validation passes
</verification>

<success_criteria>
- Search script for Living Archive
- Desktop launcher for unified view
- Uses ripgrep for fast searching
</success_criteria>

<output>
Create `.planning/phases/04-content-recognition/04-02-SUMMARY.md` when done
</output>