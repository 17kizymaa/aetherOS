---
phase: 01-foundation-build-system
plan: 05
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - README.md
  - VERSION
  - CHANGELOG.md
autonomous: true
requirements:
  - DOC-04
  - BOOT-03
user_setup: []

must_haves:
  truths:
    - "README.md quick-start instructions match actual script names and commands"
    - "VERSION file contains the exact version string 0.1.0-demo.1"
    - "CHANGELOG.md has a demo entry documenting the Phase 1 deliverables"
  artifacts:
    - path: "README.md"
      provides: "Quick-start instructions for build and validation"
    - path: "VERSION"
      provides: "Version string for the demo release"
    - path: "CHANGELOG.md"
      provides: "Changelog with demo entry"
  key_links:
    - from: "README.md"
      to: "scripts/validate.sh"
      via: "Quick-start references validate.sh"
    - from: "README.md"
      to: "scripts/build-iso.sh"
      via: "Quick-start references build-iso.sh"
    - from: "README.md"
      to: "scripts/smoke-qemu.sh"
      via: "Quick-start references smoke-qemu.sh"
---

<objective>
Update documentation (README.md, VERSION, CHANGELOG.md) to reflect the actual build system created in Plans 01-04. This fulfills DOC-04 (README.md contains working quick-start) and BOOT-03 (boot is reproducible from clean clone using build scripts).

Purpose: Documentation is part of the build system. The README.md quick-start must match actual script names so a fresh clone can `./scripts/validate.sh && ./scripts/build-iso.sh && ./scripts/smoke-qemu.sh out/*.iso` without guessing.
Output: Updated README.md, created VERSION, updated CHANGELOG.md -- all reflecting the Phase 1 deliverables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-build-system/01-CONTEXT.md
@.planning/phases/01-foundation-build-system/01-RESEARCH.md
@README.md
@CHANGELOG.md
@docs/SPRINT_OPERATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md quick-start instructions</name>
  <files>README.md</files>
  <action>Update `README.md` to ensure the quick-start section matches the actual build scripts and their usage (DOC-04). The README already has a quick-start section that references the correct script names, but it needs to be verified and updated for completeness.

Required quick-start content:
1. **Prerequisites:** Arch Linux host, with install command: `sudo pacman -Syu --needed archiso qemu-full edk2-ovmf shellcheck git jq`
2. **Validate:** `./scripts/validate.sh` -- runs G0-G3 validation
3. **Build:** `./scripts/build-iso.sh` -- produces ISO in `out/`
4. **Smoke test:** `./scripts/smoke-qemu.sh out/*.iso` -- boots ISO in QEMU with 2 GB RAM
5. **Collect artifacts:** `./scripts/collect-artifacts.sh` -- generates SHA256 and manifest
6. **Profile location:** `profiles/aetheros/` is the single source of truth for the ISO

Also update the project structure section to include:
- `profiles/aetheros/` in the structure diagram
- `artifacts/releases/` tree
- `VERSION` and `CHANGELOG.md` files

The README must NOT reference any Debian-specific commands (no `apt`, no `live-build`). Per D-07, we use Arch + pacman exclusively.

Per SPRINT_OPERATIONS.md section 1: "The root README.md should contain only the shortest reliable path: install deps, validate, build, boot in VM."</action>
  <verify>
    <automated>test -f README.md &amp;&amp; grep -q 'validate.sh' README.md &amp;&amp; grep -q 'build-iso.sh' README.md &amp;&amp; grep -q 'smoke-qemu.sh' README.md &amp;&amp; grep -q 'collect-artifacts.sh' README.md &amp;&amp; grep -q 'pacman' README.md &amp;&amp; grep -q 'profiles/aetheros' README.md &amp;&amp; ! grep -q 'apt' README.md</automated>
  </verify>
  <done>README.md quick-start references all existing scripts with correct install command (pacman, not apt), validates the build path, and matches the actual project structure. No Debian-specific references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Create VERSION and update CHANGELOG.md</name>
  <files>VERSION, CHANGELOG.md</files>
  <action>Create `VERSION` and update `CHANGELOG.md` for the Phase 1 demo.

Step 1 — Create `VERSION` file containing exactly one line:
```
0.1.0-demo.1
```
This must match `iso_version` in `profiles/aetheros/profiledef.sh` and the artifact directory name `artifacts/releases/v0.1.0-demo.1/`.

Step 2 — Update `CHANGELOG.md` to add a section for the v0.1.0-demo.1 release. Follow the existing format (## version headings with bullet points). Add under `## Unreleased`:

```markdown
## v0.1.0-demo.1

- Resolve base system decision: Arch Linux + mkarchiso (ADR-0004)
- Create mkarchiso profile `profiles/aetheros/` with minimal XFCE system
- Implement build scripts: validate.sh, validate-profile.sh, print-build-env.sh, build-iso.sh, smoke-qemu.sh, collect-artifacts.sh
- Archive Debian artifacts to deprecated/
- Update documentation to Arch Linux base
```

Per CHANGELOG.md existing format: use conventional commit types (`feat:`, `fix:`, `docs:`, `build:`, `chore:`) as prefixes for entries.

Step 3 — Verify VERSION matches profiledef.sh:
```bash
grep iso_version profiles/aetheros/profiledef.sh | grep -o '"[^"]*"' | tr -d '"'
```
Should output: `0.1.0-demo.1`</action>
  <verify>
    <automated>test -f VERSION &amp;&amp; grep -q '0.1.0-demo.1' VERSION &amp;&amp; grep -q 'v0.1.0-demo.1' CHANGELOG.md &amp;&amp; grep -q 'ADR-0004' CHANGELOG.md &amp;&amp; grep -q 'mkarchiso' CHANGELOG.md</automated>
  </verify>
  <done>VERSION contains "0.1.0-demo.1" matching profiledef.sh iso_version. CHANGELOG.md has a v0.1.0-demo.1 section documenting Phase 1 deliverables: base system decision, profile creation, build scripts, Debian archival, docs update.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> Build system | README.md instructions drive user actions on the build host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Repudiation | CHANGELOG.md | mitigate | Formal changelog entry records what was built in Phase 1 |
| T-05-02 | Information Disclosure | README.md | mitigate | No secrets, API keys, or private paths in documentation |
| T-05-0C | Tampering | npm/pip/cargo installs | mitigate | No packages installed in this plan (all file creation/editing) |
</threat_model>

<verification>
- `test -f VERSION` and `cat VERSION` outputs "0.1.0-demo.1"
- `grep 'v0.1.0-demo.1' CHANGELOG.md` finds the release section
- `grep 'validate.sh' README.md` finds the quick-start reference
- `grep 'pacman' README.md` finds install instructions
- `grep 'apt' README.md` returns nothing (no Debian references)
- VERSION string matches profiledef.sh iso_version value
</verification>

<success_criteria>
- README.md quick-start works from clean clone: install deps -> validate -> build -> smoke test
- VERSION file contains "0.1.0-demo.1"
- CHANGELOG.md documents Phase 1 deliverables under v0.1.0-demo.1
- No Debian-specific references remain in README.md
- DOC-04 requirement met: README.md contains working quick-start instructions
</success_criteria>

<output>
Create `.planning/phases/01-foundation-build-system/01-05-SUMMARY.md` when done
</output>
