---
phase: 01-foundation-build-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - docs/decisions/ADR-0004-use-archiso.md
  - deprecated/README.md
  - OPERATING_CONTRACT.md
autonomous: true
requirements:
  - DOC-04
user_setup: []

must_haves:
  truths:
    - ".gitignore prevents build artifacts from being committed"
    - "ADR-0004 formally resolves the Debian/Arch contradiction"
    - "Debian artifacts are archived to deprecated/ with explanatory README"
    - "OPERATING_CONTRACT.md references Arch Linux, not Debian"
  artifacts:
    - path: ".gitignore"
      provides: "Git ignore rules for build artifacts"
      contains: "work/"
    - path: "docs/decisions/ADR-0004-use-archiso.md"
      provides: "Arch ISO base decision record"
      contains: "Arch Linux + mkarchiso"
    - path: "deprecated/README.md"
      provides: "Explanation of archived Debian artifacts"
    - path: "OPERATING_CONTRACT.md"
      provides: "Updated operating contract"
      pattern: "Arch"
  key_links:
    - from: ".gitignore"
      to: "profiles/aetheros/"
      via: "prevents work/out/logs from being committed"
    - from: "docs/decisions/ADR-0004-use-archiso.md"
      to: "profiles/aetheros/"
      via: "mandates mkarchiso profile as build source"
---

<objective>
Resolve the critical Debian/Arch contradiction (D-01), establish git hygiene (D-08 prerequisite), and archive Debian artifacts cleanly (D-07). This plan unblocks all downstream work by creating a clean foundation: no contradictory documentation, no risk of committing build artifacts, and a formal ADR documenting the Arch Linux decision.

Purpose: The project has two incompatible build pathways (Debian/live-build vs Arch/mkarchiso) that will cause build failures if not resolved first. This must be the first work done.
Output: .gitignore, ADR-0004, archived Debian artifacts, updated OPERATING_CONTRACT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-build-system/01-CONTEXT.md
@.planning/phases/01-foundation-build-system/01-RESEARCH.md
@.planning/phases/01-foundation-build-system/01-PATTERNS.md
@OPERATING_CONTRACT.md
@adr/0001-base-system.md
@manifests/packages.base.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .gitignore to prevent artifact commits</name>
  <files>.gitignore</files>
  <action>Create `.gitignore` at the repository root with the following content. This MUST be the first file created in the project to prevent accidental commits of build artifacts (ISO files can be 500+ MB).

```
work/
out/
logs/
*.iso
*.img
*.qcow2
*.log
.env
.env.*
!.env.example
```

Per RESEARCH.md Pitfall 2 (HIGH confidence) and SPRINT_OPERATIONS.md section 1: the project currently has zero .gitignore, meaning any build attempt will generate artifacts that should not be committed.

The `work/`, `out/`, and `logs/` entries prevent mkarchiso working directories and outputs. The `.iso`, `.img`, `.qcow2` patterns prevent binary images. The `.env` pattern prevents secrets but allows `.env.example` (negation rule `!.env.example`).</action>
  <verify>
    <automated>test -f .gitignore &amp;&amp; grep -q '^work/$' .gitignore &amp;&amp; grep -q '^out/$' .gitignore &amp;&amp; grep -q '*.iso' .gitignore &amp;&amp; grep -q '.env' .gitignore &amp;&amp; grep -q '!.env.example' .gitignore</automated>
  </verify>
  <done>.gitignore exists at repo root with all required patterns: work/, out/, logs/, *.iso, *.img, *.qcow2, *.log, .env, .env.*, !.env.example</done>
</task>

<task type="auto">
  <name>Task 2: Create ADR-0004 resolving Debian/Arch contradiction</name>
  <files>docs/decisions/ADR-0004-use-archiso.md</files>
  <action>Create `docs/decisions/ADR-0004-use-archiso.md` that supersedes ADR-0001 and formally documents the Arch Linux + mkarchiso base decision (D-01).

Use `adr/0001-base-system.md` as the ADR format template (Decision/Rationale/Consequences sections).

Content requirements:
- Title: "ADR 0004 — Use Arch Linux + mkarchiso as base system"
- Decision: one sentence stating Arch Linux + mkarchiso is the build base for v0.1
- Rationale: at least 3 bullet points covering mkarchiso purpose-built ISO creation, Arch Wiki documentation quality, and the weight of project documentation (SPRINT_OPERATIONS.md, BUILD_PIPELINE.md, RESEARCH.md) all specifying Arch
- Consequences: ADR-0001 is superseded, OPERATING_CONTRACT.md must be updated, manifests/ content is invalid, bootstrap-aether.sh is invalid
- Status: "Accepted (supersedes ADR-0001)"

Follow the established commit style: `docs(adr): create ADR-0004 resolving base system decision`

Reference decision ID D-01 in the action trace.</action>
  <verify>
    <automated>test -f docs/decisions/ADR-0004-use-archiso.md &amp;&amp; grep -q 'Arch Linux' docs/decisions/ADR-0004-use-archiso.md &amp;&amp; grep -q 'mkarchiso' docs/decisions/ADR-0004-use-archiso.md &amp;&amp; grep -q 'supersedes\|Superseded\|supersede' docs/decisions/ADR-0004-use-archiso.md</automated>
  </verify>
  <done>ADR-0004 exists at docs/decisions/ADR-0004-use-archiso.md with Decision/Rationale/Consequences sections, references Arch Linux + mkarchiso, and notes ADR-0001 is superseded</done>
</task>

<task type="auto">
  <name>Task 3: Archive Debian artifacts and update OPERATING_CONTRACT.md</name>
  <files>deprecated/README.md, OPERATING_CONTRACT.md</files>
  <action>Archive all Debian-specific artifacts to `deprecated/` and update `OPERATING_CONTRACT.md` to reference Arch instead of Debian (D-07).

Step 1 — Create deprecated/ directory and move Debian artifacts:
```
mkdir -p deprecated
mv manifests/ deprecated/manifests/
mv scripts/bootstrap-aether.sh deprecated/
```

Step 2 — Create `deprecated/README.md` explaining:
- These artifacts were from the original Debian/live-build approach (ADR-0001)
- Superseded by ADR-0004 (Arch Linux + mkarchiso)
- Preserved for reference but not part of the active build path
- The bootstrap-aether.sh script requires `apt` and targets Debian package names

Step 3 — Update `OPERATING_CONTRACT.md` line 9:
- CHANGE `Debian Stable + XFCE is the default base for this sprint.` TO `Arch Linux + XFCE is the default base for this sprint.`
- This is the only text change needed in the file

Per RESEARCH.md Pitfall 1: the manifests/ directory contains Debian package names (apt-utils, firefox-esr) that will cause pacman failures if referenced. The bootstrap-aether.sh uses `apt` commands.</action>
  <verify>
    <automated>test -f deprecated/README.md &amp;&amp; test -f deprecated/manifests/packages.base.txt &amp;&amp; test -f deprecated/bootstrap-aether.sh &amp;&amp; ! test -d manifests/ &amp;&amp; grep -q 'Arch Linux' OPERATING_CONTRACT.md &amp;&amp; ! grep -q 'Debian Stable' OPERATING_CONTRACT.md</automated>
  </verify>
  <done>manifests/ and bootstrap-aether.sh moved to deprecated/, deprecated/README.md explains the archival rationale, OPERATING_CONTRACT.md references "Arch Linux" not "Debian Stable"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build host -> git repo | Build scripts write artifacts; .gitignore prevents repo bloat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .gitignore | mitigate | Excludes .env files, .iso, .img from commits |
| T-01-02 | Repudiation | ADR-0004 | mitigate | Formal decision record supersedes ADR-0001 with git history |
| T-01-0C | Tampering | npm/pip/cargo installs | mitigate | No packages installed in this plan (all file creation and mv) |
</threat_model>

<verification>
- `test -f .gitignore` passes
- `git status --short` shows .gitignore as untracked (new)
- `docs/decisions/` directory and ADR-0004 file exist
- `deprecated/` directory contains manifests/ and bootstrap-aether.sh
- `manifests/` no longer exists at repo root
- `OPERATING_CONTRACT.md` contains "Arch Linux" not "Debian Stable"
</verification>

<success_criteria>
- .gitignore prevents commit of work/, out/, logs/, *.iso, *.img, *.qcow2, *.log, .env files
- ADR-0004 formally resolves the Debian/Arch contradiction
- Debian artifacts archived with explanatory README
- OPERATING_CONTRACT.md updated to Arch
- Clean git status (no deleted files showing as unstaged -- mv should show correctly)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-build-system/01-01-SUMMARY.md` when done
</output>
