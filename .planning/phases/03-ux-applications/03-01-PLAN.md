---
phase: 03-ux-applications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - scripts/verify-ux.sh
  - docs/UX_VALIDATION.md
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - AI-01
  - AI-02

must_haves:
  truths:
    - ".env.example exists at repository root"
    - ".env.example contains API variable templates for future AI integration"
    - ".env.example does not break boot or UX when missing/misconfigured"
  artifacts:
    - path: ".env.example"
      provides: "Template for API environment variables"
      contains: "API"
    - path: "scripts/verify-ux.sh"
      provides: "Script to verify core apps launch and are responsive"
      contains: "xfce4-terminal"
  key_links:
    - from: ".env.example"
      to: "AI-01, AI-02 requirements"
      via: "Documents API variables that AI integration would use"
      pattern: "API"
    - from: "verify-ux.sh"
      to: "demo user session"
      via: "Tests that apps are responsive in the booted system"
      pattern: "xfce4-terminal"
---

<objective>
Create .env.example for AI integration and a UX verification script for core applications. The .env.example supports graceful degradation (AI-02) while verify-ux.sh tests that terminal, file manager, and text editor launch correctly.
</purpose>
Output: .env.example, verify-ux.sh, UX validation documentation</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ux-applications/03-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Create .env.example for AI integration</name>
  <files>.env.example</files>
  <action>
Create `.env.example` at repository root with API variable templates. This documents what environment variables would be needed for future AI integration without actually enabling any AI features.

Per AI-02: Missing API credentials must not break boot or UX. This file is documentation only.

```text
# .env.example
# Environment variables for aetherOS AI integration
# Copy this file to .env and fill in values for local AI features

# Ollama API (for local inference)
# OLLAMA_API_URL=http://localhost:11434

# OpenAI-compatible API (alternative)
# OPENAI_API_KEY=

# Anthropic API (for Claude integration)
# ANTHROPIC_API_KEY=

# Not used in v0.1.0-demo.1
# Features work offline without these
```
  </action>
  <verify>
    <automated>cd /home/anphuni/aetherOS && test -f .env.example && grep -q 'API' .env.example && git check-ignore .env 2>/dev/null || echo "Warning: .env not in gitignore"</automated>
  </verify>
  <done>.env.example exists with API variable templates, supports graceful degradation</done>
</task>

<task type="auto">
  <name>Create UX verification script</name>
  <files>scripts/verify-ux.sh</files>
  <action>
Create `scripts/verify-ux.sh` to verify core apps launch and are responsive. This script can be run in the VM to confirm:
- xfce4-terminal opens and responds
- thunar opens and responds
- mousepad opens and responds
- README is accessible from desktop

Follow the shell script boilerplate pattern from PATTERNS.md.
  </action>
  <verify>
    <automated>cd /home/anphuni/aetherOS && test -f scripts/verify-ux.sh && test -x scripts/verify-ux.sh && grep -q 'xfce4-terminal' scripts/verify-ux.sh && grep -q 'thunar' scripts/verify-ux.sh && grep -q 'mousepad' scripts/verify-ux.sh</automated>
  </verify>
  <done>verify-ux.sh exists and tests terminal, file manager, text editor, and README accessibility</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.example -> runtime | File is documentation only, not sourced at boot. No risk of breaking UX. |
| verify-ux.sh -> VM | Script runs inside VM to test apps. No privilege escalation needed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | .env.example | accept | File contains placeholders only, no real secrets. Committed to repo. |
| T-03-02 | Tampering | verify-ux.sh | mitigate | Script is committed to repo, reviewed. No runtime modification. |
</threat_model>

<verification>
After both tasks:
1. `.env.example` exists with API variable templates
2. `scripts/verify-ux.sh` exists, is executable, and tests core apps
3. `.env` is in .gitignore (check via git check-ignore)
</verification>

<success_criteria>
- .env.example exists at repository root
- .env.example contains API variable templates
- .env.example not required at runtime (graceful degradation)
- verify-ux.sh tests terminal, file manager, text editor
- verify-ux.sh tests README accessibility
</success_criteria>

<output>
Create `.planning/phases/03-ux-applications/03-01-SUMMARY.md` when done
</output>