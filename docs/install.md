# Install

Suggested quick path (Plan B):

1. Install Debian Stable + XFCE or Xubuntu LTS.
2. Clone this repository.
3. Run `sudo bash scripts/bootstrap-aether.sh` to install the aetherOS layer.

Plan A and C are for ISO and VM images respectively; see image/live-build/ for details.
