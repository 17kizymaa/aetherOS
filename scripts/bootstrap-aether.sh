#!/usr/bin/env bash
# bootstrap-aether.sh — bootstrap aetherOS layer on Debian Stable + XFCE
# NOTE: run as root or with sudo
set -euo pipefail

echo "Updating apt and installing base packages..."
# apt update && apt upgrade -y
# xargs -a /path/to/manifests/packages.base.txt apt install -y

# Placeholder: install desktop shortcuts, project templates, enable services

echo "[TODO] Implement bootstrap steps in scripts/bootstrap-aether.sh"
